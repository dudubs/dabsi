export class LocationStateEvent {
  constructor(public key: string, public value: any) {}
}

// useLocationState().setState("key", ...)

export class LocationState {
  /// useLocationState("asdsad")
}

// useLocationState
